run_pca <- function(features, num_pcs){
  return(NULL)
}

pca_transform_images <- function(pca, images){
  return(NULL)
}

diagnostic_pca_cloud <- function(train_features, new_features){
  return(NULL)
}
